
// Knight rider/scrolling message

// Split string into array 
const strArray = "Hello World".split()

// make a function that makes an array of length n 
function makeArray(n, str) {
	const arr = []
	for (let i = 0; i < n; i += 1) {
		arr.push(str)
	}
	return arr
}

const display = makeArray(12, '0')

// Display your array in the

// 

function mapArrayto(targetArray, sourceArray, startIndex) {
	for (let i = 0; i < targetArray.length; i += 1) {
		
	}
}